docker run -d \
  -p 5000:5000 \
  --restart=always \
  --name registry \
  -v $HOME/registry:/var/lib/registry \
  registry:2

{
echo {"\"insecure-registries\" : [\"worker1:5000\"]}" | sudo tee /etc/docker/daemon.json && sudo systemctl restart docker